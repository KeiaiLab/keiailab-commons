# Dependency Audit — 2026-05 (keiailab-commons)

Monthly dependency audit log per the dependency audit policy.
Updated when `go.mod` / `go.sum` changes.

## Direct dependencies

| Module | Version | License | Notes |
|---|---|---|---|
| `k8s.io/api` | v0.36.1 | Apache-2.0 | Kubernetes API types |
| `k8s.io/apimachinery` | v0.36.1 | Apache-2.0 | meta/v1 + API utility |
| `k8s.io/utils` | v0.0.0-20260210 | Apache-2.0 | Utility (ptr, etc.) |
| `sigs.k8s.io/controller-runtime` | v0.24.1 | Apache-2.0 | Used only by `pkg/pvc` (ADR-0016) |

`go` directive: **1.26.0**. AGPL/BUSL transitive dependency: **0** (ADR-0001 charter goal).

## Changes this month

### 2026-05-26 (audit refresh)

- Upgraded: `k8s.io/api` v0.35.0 → v0.36.1, `k8s.io/apimachinery` v0.35.0 → v0.36.1
- Go directive: 1.25.7 → 1.26.0
- Added: `sigs.k8s.io/controller-runtime` v0.24.1 (required by `pkg/pvc` — ADR-0016)
- `go mod tidy` result: drift = 0 (clean)

### Renovate policy

- `renovate.json` — `k8s.io/*` group synchronized upgrades (semver).
- Major version bumps require a manual PR (with RFC or ADR).

## License audit

Per minor release (`v0.X.0`), verify:

```bash
go mod download
go-licenses report ./... --template=licenses.tmpl > /tmp/licenses.txt
# AGPL / BUSL / SSPL grep — must match 0
```

## References

- License policy: [ADR-0001](../adr/0001-charter.md) charter ("MIT only, AGPL/BUSL transitive 0").
