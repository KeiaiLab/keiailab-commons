# Dependency Audit — 2026-05 (operator-commons)

본 문서는 의존성 감사 정책의 월별 의존성 감사 로그다.
go.mod / go.sum 변경 시점에 갱신.

## Direct dependencies

| 모듈 | 버전 | 라이선스 | Sonatype Trust | 비고 |
|---|---|---|---|---|
| `k8s.io/api` | v0.35.0 | Apache-2.0 | (검증 필요 — Renovate PR 시) | K8s API types |
| `k8s.io/apimachinery` | v0.35.0 | Apache-2.0 | 동일 | meta/v1 + apis util |
| `k8s.io/utils` | v0.0.0-20251222 | Apache-2.0 | 동일 | utility (ptr 등) |

`go` directive: 1.25.7. AGPL/BUSL transitive dependency: **0 건** (ADR-0001 charter §v0.x 목표).

## 본 월 변경

### 2026-05-09 (audit downstream consumer cross-cut)
- 추가: 없음 (`pkg/status` + `pkg/finalizer` 신규는 *std slices* 만 사용 — Go 1.21+ 표준 라이브러리)
- 제거: 없음
- 업그레이드: 없음
- `go mod tidy` 결과: drift 없음 (clean)

### Renovate 정책
- `renovate.json` 보유 — k8s.io/* 그룹 동기 업그레이드 (semver)
- Major version bump 는 *수동 PR* (RFC 또는 ADR 동반)

## License 감사

매 minor release (`v0.X.0`) 시점에 다음을 검증:

```fish
go mod download
go-licenses report ./... --template=licenses.tmpl > /tmp/licenses.txt
# AGPL / BUSL / SSPL grep — 매치 0 건 의무
```

(현재 `go-licenses` 미설치 — Makefile audit 타겟 보강 후속.)

## Refs

- 라이선스 정책: ADR-0001 charter §v0.x ("Apache-2.0 only, AGPL/BUSL transitive 0 건")
- 글로벌 표준: 거버넌스 정책의 의존성 감사 절차
